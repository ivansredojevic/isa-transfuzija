spring:
  
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL9Dialect
#        format_sql: true
        
#    show-sql: true
    defer-datasource-initialization: true
  
  datasource:
    url: jdbc:postgresql://localhost:5432/isaautogen
    username: postgres
    password: 1234
    driverClassName: org.postgresql.Driver
  
  sql:
    init:
      mode: always 
    
  mail:
    properties:
      mail:
        smtp:
          starttls:
            enable: true
          auth: true
    host: smtp.gmail.com
    username: sokolicc.dji.acc@gmail.com
    password: dmpcnamghlyqqpto
    port: '587'
    
server:
  port : 8089
 
